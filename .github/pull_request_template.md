## What changes

<!-- What does this change for the user, and why? Link the issue: "Fixes #…" -->

## How it was verified

<!-- Commands you ran and their result; screenshots for UI changes. -->

## Checklist

- [ ] `npm run typecheck`, `npm run lint` and `npm test` pass
- [ ] Tests cover the behaviour that could break, asserting observable results
- [ ] User-facing text is in both `messages.ru.ts` and `messages.en.ts`; the skill variants stay in step (`npm run plugins` after skill changes)
- [ ] `CHANGELOG.md` has a line under `## Unreleased` if users will notice the change
